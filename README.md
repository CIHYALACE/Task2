How to remove brnaches locally and remotly:

first:Locally
-you need to be in the main branch first.
- use command :git branch -d "branchName".

  second :remotly
  -git push origin --delete "branchName"
